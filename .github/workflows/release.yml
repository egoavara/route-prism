name: release

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Release tag to (re)build, e.g. v0.1.0. Must already exist as a GitHub Release.'
        required: true
        type: string
      prerelease:
        description: 'Treat as prerelease (skips :latest image tag).'
        required: false
        type: boolean
        default: true

permissions:
  contents: write       # upload release assets
  packages: write       # push to ghcr.io

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}              # egoavara/route-prism
  CHART_OCI_REPO: ghcr.io/${{ github.repository_owner }}/charts
  GO_VERSION: '1.25'
  NODE_VERSION: '24'
  # Force JS-based actions (checkout, upload-artifact, etc.) to run on
  # Node.js 24 instead of the deprecated Node.js 20 runtime.
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  meta:
    name: Resolve release metadata
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.meta.outputs.version }}
      version_no_v: ${{ steps.meta.outputs.version_no_v }}
      prerelease: ${{ steps.meta.outputs.prerelease }}
    steps:
      - id: meta
        env:
          INPUT_VERSION: ${{ inputs.version }}
          INPUT_PRERELEASE: ${{ inputs.prerelease }}
          EVENT_PRERELEASE: ${{ github.event.release.prerelease }}
        run: |
          if [ -n "${INPUT_VERSION}" ]; then
            tag="${INPUT_VERSION}"
            prerelease="${INPUT_PRERELEASE:-true}"
          else
            tag="${GITHUB_REF_NAME}"
            prerelease="${EVENT_PRERELEASE:-false}"
          fi
          echo "version=${tag}" >> "$GITHUB_OUTPUT"
          echo "version_no_v=${tag#v}" >> "$GITHUB_OUTPUT"
          echo "prerelease=${prerelease}" >> "$GITHUB_OUTPUT"

  web:
    name: Build web bundles
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.version || github.ref }}

      - uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: |
            web/pnpm-lock.yaml
            web/widget/pnpm-lock.yaml

      - name: Build dashboard + widget bundles
        run: |
          (cd web && pnpm install --frozen-lockfile && pnpm build)
          (cd web/widget && pnpm install --frozen-lockfile && pnpm build)

      - uses: actions/upload-artifact@v4
        with:
          name: web-dist
          path: |
            internal/dashboard/dist
            internal/widget/dist
          if-no-files-found: error
          retention-days: 1

  build-binary:
    name: Build (${{ matrix.goos }}/${{ matrix.goarch }})
    runs-on: ubuntu-latest
    needs: [meta, web]
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
            ext: ''
          - goos: linux
            goarch: arm64
            ext: ''
          - goos: windows
            goarch: amd64
            ext: '.exe'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.version || github.ref }}

      - name: Restore web bundles
        uses: actions/download-artifact@v4
        with:
          name: web-dist
          # upload-artifact computed `internal/` as the common ancestor and
          # stripped it, so we restore under that same prefix here.
          path: internal

      - name: Verify web bundles present
        run: |
          test -f internal/dashboard/dist/index.html
          test -d internal/widget/dist

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Build operator binary
        env:
          CGO_ENABLED: '0'
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          mkdir -p dist
          go build -trimpath \
            -ldflags="-s -w -X main.version=${{ needs.meta.outputs.version }}" \
            -o "dist/manager_${GOOS}_${GOARCH}${{ matrix.ext }}" \
            ./cmd

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: manager-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/manager_${{ matrix.goos }}_${{ matrix.goarch }}${{ matrix.ext }}
          retention-days: 1

      - name: Attach binary to GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          asset="route-prism-operator_${{ needs.meta.outputs.version_no_v }}_${{ matrix.goos }}_${{ matrix.goarch }}${{ matrix.ext }}"
          cp "dist/manager_${{ matrix.goos }}_${{ matrix.goarch }}${{ matrix.ext }}" "${asset}"
          sha256sum "${asset}" > "${asset}.sha256"
          gh release upload "${{ needs.meta.outputs.version }}" \
            "${asset}" "${asset}.sha256" --clobber

  docker:
    name: Build & push image (multi-arch)
    runs-on: ubuntu-latest
    needs: [meta, build-binary]
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.version || github.ref }}

      - name: Download linux/amd64 binary
        uses: actions/download-artifact@v4
        with:
          name: manager-linux-amd64
          path: dist

      - name: Download linux/arm64 binary
        uses: actions/download-artifact@v4
        with:
          name: manager-linux-arm64
          path: dist

      - name: Make binaries executable
        run: |
          chmod +x dist/manager_linux_amd64
          chmod +x dist/manager_linux_arm64
          ls -la dist/

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - name: Login to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract image metadata
        id: img
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable=${{ needs.meta.outputs.prerelease == 'false' }}

      - name: Build & push multi-arch image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.release
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.img.outputs.tags }}
          labels: ${{ steps.img.outputs.labels }}
          provenance: false

  installer:
    name: Generate install.yaml
    runs-on: ubuntu-latest
    needs: [meta, docker]
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.version || github.ref }}

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Build installer manifest
        env:
          IMG: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.meta.outputs.version_no_v }}
        run: |
          make build-installer IMG="${IMG}"
          mv dist/install.yaml "dist/route-prism-${{ needs.meta.outputs.version_no_v }}.yaml"

      - name: Attach install.yaml to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          asset="dist/route-prism-${{ needs.meta.outputs.version_no_v }}.yaml"
          gh release upload "${{ needs.meta.outputs.version }}" \
            "${asset}" --clobber

  helm:
    name: Package & publish Helm chart
    runs-on: ubuntu-latest
    needs: [meta, docker]
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.version || github.ref }}

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - uses: azure/setup-helm@v4
        with:
          version: v3.16.4

      - name: Stage CRDs into chart
        run: |
          make manifests
          mkdir -p charts/route-prism/crds
          cp config/crd/bases/*.yaml charts/route-prism/crds/

      - name: Set chart version & appVersion
        env:
          VER: ${{ needs.meta.outputs.version_no_v }}
        run: |
          sed -i -E "s/^version: .*/version: ${VER}/" charts/route-prism/Chart.yaml
          sed -i -E "s/^appVersion: .*/appVersion: ${VER}/" charts/route-prism/Chart.yaml
          cat charts/route-prism/Chart.yaml

      - name: Lint chart
        run: helm lint charts/route-prism

      - name: Package chart
        run: |
          mkdir -p dist
          helm package charts/route-prism --destination dist

      - name: Login to ghcr.io (helm)
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | \
            helm registry login ${{ env.REGISTRY }} \
              --username ${{ github.actor }} --password-stdin

      - name: Push chart to ghcr (OCI)
        run: |
          helm push "dist/route-prism-${{ needs.meta.outputs.version_no_v }}.tgz" \
            "oci://${{ env.CHART_OCI_REPO }}"

      - name: Attach chart .tgz to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ needs.meta.outputs.version }}" \
            "dist/route-prism-${{ needs.meta.outputs.version_no_v }}.tgz" \
            --clobber
