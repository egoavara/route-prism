# This patch adds the args to allow exposing the controller-runtime metrics
# endpoint using HTTPS. Inserted after the `run` subcommand at index 0.
- op: add
  path: /spec/template/spec/containers/0/args/1
  value: --metrics.bind-address=:8443
